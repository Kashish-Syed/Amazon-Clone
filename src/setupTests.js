// Runs before every test file. Create React App picks this up automatically.
//
// jest-dom adds DOM-aware matchers - toBeInTheDocument, toBeDisabled,
// toHaveTextContent - so assertions read as intent rather than as poking at
// node properties.
import '@testing-library/jest-dom';
