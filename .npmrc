; react-currency-format@1.1.0 declares a peer range of React <=17, but this app
; runs React 18. The library works fine at runtime; only the peer metadata is stale.
; Without this, a clean `npm install` fails with ERESOLVE.
legacy-peer-deps=true
